# vue3biga 架构规范 V2

> **项目路线：纯 Vue3 / Vite / Pinia / Vue Router / ES Module /
> Supabase**
>
> 本文件不是"迁移阶段说明"，而是项目进入持续开发阶段后的**长期架构红线与
> Bug 修复规范**。
>
> 核心思想保持不变：
>
> ``` text
> UI → Logic → Data → Supabase/API
> ```
>
> 但规范从"如何完成 Vue3 迁移"升级为：
>
> **如何保证以后修
> Bug、加功能、重构代码时，永远不会重新走回旧架构，也不会因为前端异常造成数据丢失、误删或静默不保存。**

------------------------------------------------------------------------

# 1. 当前阶段定义

项目已经进入：

> **纯 Vue3 运行阶段 / 持续维护阶段**

因此以后禁止再以：

``` text
“为了兼容旧代码”
“先让它跑起来”
“临时挂到 window”
“先恢复旧 JS”
```

作为解决方案。

如果旧功能没有迁移完成：

> **必须把旧功能继续迁移成 Vue3 + ES Module，而不是恢复旧架构。**

------------------------------------------------------------------------

# 2. 核心架构

项目唯一推荐的数据与业务方向：

``` text
┌─────────────────────────────┐
│           Vue UI             │
│ View / Component / Template  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│          Pinia /             │
│      Composable / Logic      │
│ 状态、业务规则、工作流、协调 │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│            Data             │
│ Supabase / API / CRUD / IO  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│       Supabase / API        │
│ Database / Storage / RT     │
└─────────────────────────────┘
```

### 依赖方向必须保持：

``` text
UI → Logic → Data → Backend
```

允许：

``` text
UI → Pinia
Logic → Pinia
Logic → Data
Data → Backend
```

禁止：

``` text
Data → UI
Data → Vue Component
Data → Router
Data → DOM
Data → window 全局业务对象

UI → Supabase 直接 CRUD
UI → document.querySelector 处理业务
UI → window.xxx 业务调用
```

------------------------------------------------------------------------

# 3. 三层职责

## 3.1 UI / View 层

目录示例：

``` text
src/views/
src/components/
```

负责：

-   页面结构
-   Template
-   CSS
-   用户点击
-   输入
-   Modal 开关
-   Loading
-   Error 展示
-   调用 Logic / Store
-   展示 Store / Composable 提供的数据

### UI 可以做：

``` js
function onEdit(id) {
    openEditModal(id)
}
```

然后：

``` text
UI
 ↓
Logic
```

### UI 不应该做：

``` js
const { data } = await supabase
    .from('stocks')
    .update(...)
```

也不能：

``` js
window.saveStock(...)
```

更不能：

``` js
document.querySelector(...)
```

来完成业务数据操作。

------------------------------------------------------------------------

# 4. Logic 层

目录示例：

``` text
src/logic/
```

负责：

-   业务规则
-   工作流
-   CRUD 流程
-   数据组合
-   日期逻辑
-   导入导出流程
-   看板刷新
-   Realtime 协调
-   数据校验
-   防误删判断
-   错误处理
-   乐观更新/回滚

例如：

``` text
用户点击保存
 ↓
Logic.validate()
 ↓
Logic.save()
 ↓
Data.update()
 ↓
Supabase
 ↓
返回结果
 ↓
更新 Store
 ↓
UI
```

Logic 不应该直接操作 DOM。

------------------------------------------------------------------------

# 5. Data 层

目录示例：

``` text
src/data/
```

负责：

-   Supabase client
-   CRUD
-   查询
-   Insert
-   Update
-   Delete
-   Realtime subscription
-   Storage
-   数据格式转换
-   数据库错误处理

Data 层必须是：

> **唯一可信的数据访问入口。**

推荐：

``` js
export async function updateStock(payload) {
    const supabase = getSupabase()

    const { data, error } = await supabase
        .from('stocks')
        .update(payload)
        .eq('id', payload.id)
        .select()
        .single()

    if (error) throw error

    return data
}
```

禁止业务组件直接写：

``` js
supabase.from(...)
```

------------------------------------------------------------------------

# 6. Pinia 使用规范

Pinia 用于：

-   跨组件共享状态
-   当前用户
-   当前日期
-   当前选中的股票
-   全局 UI 状态
-   Realtime 更新后的共享状态
-   跨看板需要共享的数据

Pinia 不应该变成：

> "第二个数据库"。

禁止把所有 Supabase 数据无条件复制一份长期存进 Store。

原则：

``` text
数据库 = 持久化真相
Pinia = 当前应用状态
Logic = 业务规则
UI = 展示
```

------------------------------------------------------------------------

# 7. EventBus 使用规范

EventBus 只能用于：

> **真正的跨模块事件通知。**

例如：

``` text
Realtime 更新
 ↓
emit('auction-refresh')
```

但不允许用 EventBus 替代正常的状态管理。

禁止：

``` text
点击编辑
 ↓
emit('stock-edit')
 ↓
另一个不知道在哪里的文件监听
 ↓
打开 Modal
```

这种结构非常容易出现：

> emit 有了，on 没了。

### 推荐：

``` text
父组件
 ↓
props / emits
 ↓
子组件
```

跨组件状态：

``` text
Pinia
```

跨业务模块通知：

``` text
EventBus
```

数据库变化：

``` text
Realtime
```

三者职责必须明确。

------------------------------------------------------------------------

# 8. 最重要：数据安全红线

## 8.1 禁止静默保存失败

任何保存函数：

``` text
save()
update()
insert()
delete()
```

都必须明确返回：

``` text
success
或
error
```

禁止：

``` js
if (something) {
    save()
}
```

然后条件不满足就：

``` js
return
```

却不告诉 UI。

尤其禁止：

``` js
typeof xxx !== 'undefined'
```

作为数据保存是否执行的条件。

------------------------------------------------------------------------

# 9. "保存成功"的定义

不能以：

``` text
UI 变了
```

作为保存成功。

必须：

``` text
UI 修改
 ↓
Logic
 ↓
Data
 ↓
Supabase 成功
 ↓
返回成功结果
 ↓
Store 更新
 ↓
UI 更新
```

如果 Supabase 保存失败：

``` text
UI 必须知道失败
```

至少需要：

``` text
toast
error state
console error
```

不能让用户看到：

> "看起来已经保存了"

但实际上没有写入数据库。

------------------------------------------------------------------------

# 10. 所有写操作必须具备结果确认

## Insert

必须确认：

``` text
insert 成功
```

## Update

必须确认：

``` text
update 成功
```

## Delete

必须确认：

``` text
delete 成功
```

## Batch 操作

必须确认：

``` text
成功数量
失败数量
```

不能：

``` text
Promise.all()
```

后完全忽略异常。

------------------------------------------------------------------------

# 11. 删除操作是最高风险操作

任何：

``` text
delete
remove
批量删除
同步删除
清空
覆盖
替换
```

都属于：

> **高风险操作。**

必须遵循：

``` text
判断 → 保护 → 执行 → 验证
```

------------------------------------------------------------------------

# 12. 禁止"空数据 = 删除云端数据"

这是以后最重要的数据库红线之一。

绝对禁止：

``` text
本地读取失败
        ↓
[]
        ↓
认为用户没有数据
        ↓
同步 []
        ↓
删除云端全部数据
```

正确逻辑：

``` text
读取失败
 ↓
ERROR
 ↓
停止同步
```

而不是：

``` text
读取失败
 ↓
空数组
 ↓
删除云端
```

------------------------------------------------------------------------

# 13. 禁止"异常快照"触发批量删除

例如：

``` text
正常云端：1000 条
本地异常：10 条
```

绝对不能认为：

``` text
用户删除了 990 条
```

必须进入：

``` text
_suspiciousWipe
```

保护逻辑。

当前项目已有批量删除防护：

> 当本地快照与云端数据差异超过阈值时，跳过批量删除并记录日志。

**该机制必须长期保留，并且以后任何人修改同步逻辑都不能删除或绕过它。**

------------------------------------------------------------------------

# 14. 删除必须具备二次验证

危险操作建议：

``` text
普通删除：
确认一次

批量删除：
确认 + 数量提示

异常大批量删除：
直接阻止 + 要求人工确认
```

例如：

``` text
即将删除 187 条数据。

当前云端共有 203 条。
删除比例：92%。

该操作可能是异常同步。

是否继续？
```

------------------------------------------------------------------------

# 15. 删除后的验证

任何批量删除都应该：

``` text
执行 delete
 ↓
检查 Supabase response
 ↓
确认 affected rows
 ↓
确认结果符合预期
```

如果：

``` text
预期删除 10
实际删除 500
```

必须：

``` text
报警 / 停止后续操作
```

------------------------------------------------------------------------

# 16. 重要数据必须考虑恢复能力

对于高价值数据：

``` text
股票记录
历史记录
交易记录
看板数据
重要配置
```

不要把：

``` text
DELETE
```

当成唯一方案。

能使用软删除时优先考虑：

``` text
deleted_at
```

例如：

``` text
正常：
deleted_at = null

删除：
deleted_at = timestamp
```

这样可以：

``` text
恢复
审计
追踪
```

对于必须物理删除的表，也必须保留：

``` text
备份 / 导出 / 数据库恢复能力
```

------------------------------------------------------------------------

# 17. 数据同步必须采用"先确认、后删除"

任何同步流程：

``` text
本地
 ↓
云端
```

不得直接：

``` text
比较
 ↓
delete
 ↓
insert
```

推荐：

``` text
读取云端
 ↓
读取本地
 ↓
校验
 ↓
计算 diff
 ↓
检查异常比例
 ↓
生成操作计划
 ↓
确认计划安全
 ↓
执行 update / insert / delete
 ↓
重新读取云端
 ↓
验证结果
```

------------------------------------------------------------------------

# 18. 所有 Migration 必须幂等

迁移脚本可能被：

``` text
刷新
重新部署
重新打开页面
重复执行
```

因此必须保证：

``` text
执行一次 = 正常

执行两次 = 结果不变

执行十次 = 结果仍然不变
```

禁止：

``` text
每次启动都重复 insert
```

更禁止：

``` text
启动时自动 delete + 重建
```

------------------------------------------------------------------------

# 19. Migration 不应该由 UI 随意触发

应用启动：

``` text
App.vue
```

不应该无限堆积：

``` text
migration
realtime
session
data loading
业务同步
```

推荐：

``` text
App
 ↓
bootstrap
 ├── session
 ├── migration
 ├── realtime
 └── initial data
```

UI 不应该知道数据库迁移细节。

------------------------------------------------------------------------

# 20. App.vue 职责

App.vue 应尽量保持轻量。

推荐：

``` vue
<template>
  <AppShell>
    <RouterView />
  </AppShell>
</template>
```

初始化：

``` js
useAppBootstrap()
```

而不是继续把：

``` text
登录
Migration
Realtime
股票同步
竞价同步
热点同步
数据迁移
VisibilityChange
```

全部堆进 App.vue。

------------------------------------------------------------------------

# 21. 一个业务模块的标准结构

推荐：

``` text
src/
├── views/
│   └── AuctionBoard.vue
│
├── components/
│   └── auction/
│
├── logic/
│   └── auction/
│       ├── auction-workflow.js
│       ├── auction-validation.js
│       └── auction-sync.js
│
├── data/
│   └── auction/
│       ├── auction-data.js
│       └── auction-realtime.js
│
└── stores/
    └── auction-store.js
```

核心思想：

``` text
一个业务模块
=
View
+
Logic
+
Data
+
必要的 Store
```

------------------------------------------------------------------------

# 22. 禁止重新出现旧架构

以后任何 Bug 修复都禁止：

``` js
window.xxx = ...
```

禁止：

``` js
window.app.xxx()
```

禁止：

``` js
window.remainingBoards
```

禁止：

``` js
(function () {
   ...
})()
```

作为业务模块。

禁止：

``` html
<script src="xxx.js"></script>
```

来绕过 ES Module。

禁止：

``` js
globalThis.xxx
```

隐藏业务状态。

------------------------------------------------------------------------

# 23. 不允许为了修 Bug 恢复旧代码

错误示范：

``` text
Vue3 新代码坏了
 ↓
把旧 boards-stocks.js 加回来
```

禁止。

正确：

``` text
发现旧功能缺失
 ↓
找到旧功能行为
 ↓
重新实现成 Vue3 SFC + Logic + Data
```

------------------------------------------------------------------------

# 24. 所有旧代码只能作为"行为参考"

旧代码可以：

``` text
查业务规则
查字段
查计算方式
查历史行为
```

不能：

``` text
直接重新挂回运行时
```

原则：

> **复用业务知识，不复活旧架构。**

------------------------------------------------------------------------

# 25. `remaining-boards.js` 特别规定

该文件已经被审查确认属于旧架构残留。

以后不得：

``` text
重新恢复 IIFE
重新恢复 window.remainingBoards
重新依赖 CDN Supabase
```

必须：

``` text
ES Module
+
getSupabase()
+
显式 import/export
```

最终目标：

``` text
remaining-boards.js
```

要么：

> 正式迁移成标准 Data/Logic 模块。

要么：

> 如果功能已经被新模块替代，则删除。

绝不能保持：

> "半旧半新"的隐藏状态。

------------------------------------------------------------------------

# 26. `app-core.js` 拆分原则

`app-core.js` 不再作为无限增长的万能业务文件。

最终应该拆成：

``` text
logic/
├── app/
├── stocks/
├── auction/
├── bidding/
├── jiwang/
├── hotspot/
├── pattern/
├── rank/
├── emotion/
├── etf/
└── stats/
```

每个模块自己负责：

``` text
业务规则
```

而不是所有业务都塞进：

``` text
app-core.js
```

------------------------------------------------------------------------

# 27. Data Source 唯一性

一个业务数据必须明确：

``` text
唯一数据库来源
```

例如：

``` text
auction_watchlist
```

就是正式竞价列表数据源。

不能出现：

``` text
A 看板读新表
B 看板读旧表
C 看 allData
D 看 localStorage
```

然后认为：

> "都差不多。"

这是数据不一致的主要来源。

------------------------------------------------------------------------

# 28. `state.allData` 使用规范

`allData` 如果只是：

``` text
历史兼容层
```

必须逐步删除。

如果仍然保留：

必须明确它属于：

``` text
cache
view model
store
```

中的哪一种。

禁止同时存在：

``` text
Supabase
allData
Pinia
local cache
组件 ref
```

五套"真相"。

------------------------------------------------------------------------

# 29. UI 响应式原则

所有用户操作：

``` text
新增
修改
删除
复制
排序
筛选
```

必须让 UI 在操作成功后立即更新。

不要依赖：

``` text
setInterval
setTimeout
重新进入页面
切换日期
```

来"碰巧刷新"。

正确：

``` text
Data 成功
 ↓
Store / reactive state 更新
 ↓
Vue 自动渲染
```

------------------------------------------------------------------------

# 30. Realtime 原则

Realtime 负责：

> **跨页面 / 跨设备 / 跨会话的数据变化通知。**

不是：

> 本页面自己的 CRUD 替代方案。

正确：

``` text
A 保存
 ↓
Supabase
 ↓
Realtime
 ↓
B 收到变化
 ↓
B Store 更新
 ↓
B UI 更新
```

本页面 A：

``` text
保存成功
 ↓
立即更新自己的 Store
```

不应该等 Realtime 才显示。

------------------------------------------------------------------------

# 31. Realtime 防重复更新

必须防止：

``` text
用户自己保存
 ↓
Store 更新一次
 ↓
Realtime 又更新一次
 ↓
重复请求
```

需要根据业务设计：

``` text
乐观更新
```

或：

``` text
服务器结果更新
```

二选一，不要两个体系混用到无法判断。

------------------------------------------------------------------------

# 32. 错误处理红线

禁止：

``` js
try {
   await save()
} catch (e) {
}
```

空 catch。

必须：

``` js
catch (error) {
   console.error(error)
   showError(...)
}
```

业务错误必须让上层知道。

------------------------------------------------------------------------

# 33. 不允许"吞错"

以下方式禁止：

``` js
catch {
   return []
}
```

如果数据库查询失败：

``` text
[]
```

不能被当成：

> "数据库没有数据。"

必须区分：

``` text
SUCCESS + []
```

和：

``` text
ERROR
```

这是防止"异常数据导致误删"的核心规则。

------------------------------------------------------------------------

# 34. 数据读取失败时的行为

如果：

``` text
Supabase 查询失败
```

UI 应显示：

``` text
加载失败
```

而不是：

``` text
空列表
```

更不能：

``` text
空列表 → 同步 → 删除云端数据
```

------------------------------------------------------------------------

# 35. 数据写入失败时的行为

如果：

``` text
Update 失败
```

必须：

``` text
保持原数据
+
提示失败
+
记录日志
```

不能：

``` text
先把 UI 改掉
然后悄悄保存失败
```

如果使用乐观更新：

``` text
失败
 ↓
rollback
```

------------------------------------------------------------------------

# 36. 批量操作安全等级

建议统一：

``` text
1 条
↓
普通操作

2～20 条
↓
普通确认

20～100 条
↓
强化确认

>100 条
↓
异常检查

>60% 数据变化
↓
默认阻止
```

具体阈值可以根据业务调整，但：

> **大规模变化必须有保护。**

当前项目已有 `_suspiciousWipe`，必须保留。

------------------------------------------------------------------------

# 37. 任何同步函数修改前必须回答 5 个问题

以后 AI 或开发人员修改：

``` text
sync
save
delete
import
migration
```

之前必须先回答：

### 1

数据从哪里来？

### 2

数据写到哪里？

### 3

什么情况下认为读取失败？

### 4

什么情况下允许删除？

### 5

失败后怎么恢复？

如果回答不清楚：

> **禁止直接修改同步代码。**

------------------------------------------------------------------------

# 38. Bug 修复流程

以后修 Bug 必须按照：

``` text
① 复现
 ↓
② 定位 View / Logic / Data
 ↓
③ 找到真实数据源
 ↓
④ 确认调用链
 ↓
⑤ 修改最小范围
 ↓
⑥ npm run build
 ↓
⑦ 功能回归
 ↓
⑧ 刷新页面验证
 ↓
⑨ 多设备/Realtime 验证
```

禁止：

``` text
看到报错
 ↓
直接加一个 if
```

------------------------------------------------------------------------

# 39. AI 修改代码的特别规则

以后让 AI 修改项目时，必须把本文件一起提供给 AI。

AI 必须遵守：

``` text
1. 不恢复旧 Vue/JS 架构
2. 不增加 window 全局 API
3. 不直接在 UI 写 Supabase
4. 不跳过 Logic 层
5. 不把业务逻辑塞进 App.vue
6. 不删除数据保护机制
7. 不把读取失败转换为空数组
8. 不让保存失败静默
9. 不绕过 Data Module
10. 不为了修一个 Bug 破坏三层架构
```

------------------------------------------------------------------------

# 40. AI 修改前必须先检查

AI 在修改前应输出：

``` text
【问题定位】
发生在哪一层？

【当前调用链】
UI → Logic → Data → Backend

【真实数据源】
是哪张表？

【影响范围】
哪些看板？

【是否涉及写入】
是 / 否

【是否涉及删除】
是 / 否

【是否涉及迁移】
是 / 否

【风险等级】
P0 / P1 / P2 / P3
```

然后再修改。

------------------------------------------------------------------------

# 41. AI 不得凭猜测修改数据库逻辑

如果 AI 不确定：

``` text
表名
字段名
数据来源
同步关系
删除规则
```

必须先搜索项目代码。

禁止：

``` text
猜一个字段
猜一个表
猜一个 API
```

------------------------------------------------------------------------

# 42. 修改 Data 层必须增加验证

修改：

``` text
insert
update
delete
sync
migration
```

后必须至少验证：

``` text
成功
失败
空数据
重复数据
异常数据
刷新
Realtime
```

------------------------------------------------------------------------

# 43. 修改删除逻辑必须额外验证

必须验证：

``` text
正常删除
批量删除
空数据删除
读取失败
网络失败
云端为空
本地为空
本地异常少
本地异常多
```

尤其：

``` text
读取失败 ≠ 空数据
```

必须成为永久红线。

------------------------------------------------------------------------

# 44. Git 提交规范

推荐：

``` text
fix:
feat:
refactor:
data:
migration:
security:
```

例如：

``` text
fix: 修复 HomeStocksView 编辑保存链路
fix: 阻止异常同步触发批量删除
refactor: 拆分 app-core stocks workflow
data: 迁移 remaining-boards 为 ES module
security: 加强批量删除保护
```

------------------------------------------------------------------------

# 45. 上线前最低验收标准

必须全部通过：

## 架构

-   [ ] 全部 UI 使用 `.vue`
-   [ ] 没有业务 `window.*`
-   [ ] 没有 `innerHTML`
-   [ ] 没有 CDN Vue
-   [ ] 没有 CDN Supabase
-   [ ] 没有旧 IIFE 业务模块
-   [ ] 没有 globalProperties 业务注入

## 数据

-   [ ] 所有核心看板能读取
-   [ ] 所有核心看板能新增
-   [ ] 所有核心看板能编辑
-   [ ] 所有核心看板能删除
-   [ ] 保存失败可见
-   [ ] 刷新后数据仍存在
-   [ ] 换设备数据仍存在

## 安全

-   [ ] 批量删除有保护
-   [ ] 异常数据不会触发清空
-   [ ] 读取失败不会变成空数据
-   [ ] 空数据不会自动删除云端
-   [ ] Migration 幂等
-   [ ] Delete 有结果确认

## 同步

-   [ ] Realtime 正常
-   [ ] 新增同步
-   [ ] 修改同步
-   [ ] 删除同步
-   [ ] 多设备同步
-   [ ] 不重复更新

## 构建

-   [ ] `npm run build`
-   [ ] 无新增 warning
-   [ ] 无循环依赖
-   [ ] 无未使用关键模块
-   [ ] 无明显 dead code

------------------------------------------------------------------------

# 46. 当前项目上线风险判断

根据当前审查结果：

## 数据"误删"风险

目前已确认：

> `auction-sync.js` 存在 `_suspiciousWipe`
> 防误删机制，并且对异常的大规模差异进行拦截。

因此：

> **当前没有证据表明项目存在"正常情况下必然大面积误删数据库"的问题。**

但是：

> **不能因此认为整个项目已经没有删除风险。**

因为其他删除/同步路径仍需要逐一验证。

------------------------------------------------------------------------

# 47. 当前项目"数据没了 / 没保存"的风险

这个风险目前是明确存在的。

已确认：

``` text
remaining-boards.js
 ↓
没有正常 import
 ↓
remainingBoards 不存在
 ↓
部分 saveData() 分支直接跳过
 ↓
数据没有真正持久化
```

因此当前更现实的风险是：

> **用户操作后界面显示成功，但刷新或换设备后发现数据没有保存。**

这属于：

> **P0 数据完整性问题。**

必须在上线前修复。

------------------------------------------------------------------------

# 48. 上线门槛

在以下条件满足之前：

``` text
remaining-boards 修复
+
HomeStocksView 修复
+
六个重点看板持久化回归
+
删除安全回归
+
Realtime 回归
```

不建议正式上线。

------------------------------------------------------------------------

# 49. 长期目标

最终项目应达到：

``` text
                Vue3
                  │
          ┌───────┴───────┐
          │               │
        Router          Pinia
          │               │
          └───────┬───────┘
                  │
                Logic
                  │
                Data
                  │
             Supabase
                  │
       ┌──────────┴──────────┐
       │                     │
    Database              Realtime
```

并且：

``` text
没有 window 业务全局变量
没有旧 IIFE
没有 CDN 依赖
没有 UI 直接操作数据库
没有静默保存失败
没有读取失败伪装为空数据
没有异常同步直接清空云端
没有未经保护的批量删除
没有无限增长的 app-core
```

------------------------------------------------------------------------

# 50. 最终原则

以后这个项目无论：

``` text
修 Bug
加功能
重构
迁移数据库
增加看板
修改同步
修改 Realtime
修改 Supabase
```

都必须遵守下面 10 条：

### ① Vue3 是唯一前端架构

不恢复旧架构。

### ② UI 只负责展示和交互

不直接操作数据库。

### ③ Logic 负责业务

业务规则不能散落在 UI。

### ④ Data 是数据库唯一入口

所有 Supabase CRUD 从 Data 层进入。

### ⑤ 数据库是持久化真相

Pinia 不是数据库。

### ⑥ 保存必须有结果

不能静默失败。

### ⑦ 读取失败绝不能当空数据

这是数据安全红线。

### ⑧ 空数据绝不能自动触发批量删除

这是数据安全红线。

### ⑨ 所有危险删除必须有护栏

尤其是批量同步。

### ⑩ 修 Bug 不能破坏架构

> **宁可补齐 Vue3 模块，也不要把旧代码重新挂回来。**

------------------------------------------------------------------------

# 51. 本文件的使用方式

建议将本文件正式保存为项目根目录：

``` text
ARCHITECTURE.md
```

以后每次修改项目时：

``` text
开发者 / AI
      ↓
先读 ARCHITECTURE.md
      ↓
定位问题
      ↓
判断 UI / Logic / Data
      ↓
修改
      ↓
执行验收
```

如果未来项目进入新的架构阶段，可以继续升级：

``` text
ARCHITECTURE.md
```

但不得降低本文件中的：

> **数据安全红线、纯 Vue3 红线、三层依赖方向、禁止旧全局架构**

这些核心原则。

------------------------------------------------------------------------

# 52. 当前阶段的一句话定义

> **vue3biga 现在不是"继续迁移 Vue3"，而是"在纯 Vue3
> 架构上完成业务闭环，并建立长期不走偏、不丢数据、不误删的数据安全体系"。**
