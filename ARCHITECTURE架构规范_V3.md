---
name: "architechure"
description: "# 项目架构规范"
---

# vue3biga 纯 Vue3 架构规范 V3

> **项目架构：纯 Vue3 / Vite / Pinia / Vue Router / ES Module / Supabase**
>
> 本文件是项目进入**长期维护 + 持续新增功能阶段**后的统一架构红线。
>
> 核心架构思想保持不变：
>
> ```text
> UI → Logic → Data → Supabase/API
> ```
>
> V3 不改变原有三层架构、数据安全、Realtime、Supabase、纯 Vue3 等核心思想。
> V3 的主要升级是：
>
> 1. 将原有重复规则重新归并；
> 2. 明确响应式、渲染、生命周期和数据更新边界；
> 3. 将性能纳入架构红线，而不是把卡顿当成单个页面 Bug；
> 4. 规范以后所有 Bug 修复和新功能开发；
> 5. 防止项目长期维护后重新演变成“大状态 + 大组件 + 全量更新 + 重复请求”的旧模式。
>
> **最终目标：功能正确、数据安全、架构清晰、响应式可控、渲染高效、长期可维护。**

------------------------------------------------------------------------

# 1. 当前阶段：纯 Vue3 维护与新增功能阶段

> **Vue3 迁移阶段已经结束。**

当前项目已经正式进入：

```text
纯 Vue3
+
长期维护
+
Bug 修复
+
持续新增功能
+
性能优化
```

因此，本规范**不再把“迁移 Vue3”作为目标**。

以后所有代码都默认运行在纯 Vue3 架构下，不再讨论：

```text
如何继续迁移 Vue3
如何兼容旧 Vue
如何保留旧 JS 架构
如何恢复旧页面
```

也禁止以：

```text
为了兼容旧代码
先让它跑起来
临时挂到 window
先恢复旧 JS
```

作为解决方案。

如果发现历史遗留代码：

> **直接按照纯 Vue3 架构进行重构或替换，而不是恢复旧架构。**

从现在开始，所有新功能与 Bug 修复的验收标准统一为：

```text
功能正确
+
数据安全
+
纯 Vue3 架构正确
+
响应式边界正确
+
性能可持续
+
长期可维护
```

------------------------------------------------------------------------

# 2. 核心架构

项目唯一推荐的数据与业务方向：

```text
┌─────────────────────────────┐
│           Vue UI             │
│ View / Component / Template  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│       Pinia / Logic /        │
│       Composable / VM        │
│ 状态、业务规则、工作流、协调 │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│            Data             │
│ Supabase / API / CRUD / IO  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│       Supabase / API        │
│ Database / Storage / RT     │
└─────────────────────────────┘
```

依赖方向必须保持：

```text
UI → Logic → Data → Backend
```

允许：

```text
UI → Pinia
Logic → Pinia
Logic → Data
Data → Backend
```

禁止：

```text
Data → UI
Data → Vue Component
Data → Router
Data → DOM
Data → window 全局业务对象

UI → Supabase 直接 CRUD
UI → document.querySelector 处理业务
UI → window.xxx 业务调用
```

------------------------------------------------------------------------

# 3. 三层职责

## 3.1 UI / View 层

目录：

```text
src/views/
src/components/
```

负责：

```text
页面结构
Template
CSS
用户点击
输入
Modal
Loading
Error 展示
调用 Logic / Store
展示 Store / Composable / ViewModel 数据
```

UI 不负责：

```text
Supabase CRUD
数据库同步
复杂业务规则
Realtime 协调
数据迁移
```

例如：

```js
function onEdit(id) {
    openEditModal(id)
}
```

然后：

```text
UI
 ↓
Logic
```

禁止：

```js
const { data } = await supabase
    .from('stocks')
    .update(...)
```

也禁止使用：

```text
window.xxx
document.querySelector()
innerHTML
```

绕过 Vue 架构完成业务操作。

------------------------------------------------------------------------

# 4. Logic / Composable / ViewModel 层

目录示例：

```text
src/logic/
src/composables/
```

负责：

```text
业务规则
工作流
CRUD 流程协调
数据组合
日期逻辑
导入导出
看板刷新
Realtime 协调
数据校验
防误删判断
错误处理
乐观更新 / 回滚
```

标准调用链：

```text
用户操作
 ↓
Logic.validate()
 ↓
Logic.save()
 ↓
Data.update()
 ↓
Supabase
 ↓
返回结果
 ↓
更新 Store / State
 ↓
UI
```

Logic 不应该直接操作 DOM。

### ViewModel 规则

复杂页面可以增加 ViewModel / 派生数据层：

```text
Data
 ↓
Logic
 ↓
ViewModel / computed
 ↓
UI
```

ViewModel 只负责把业务数据整理成 UI 所需要的结构，不成为第二个数据源。

------------------------------------------------------------------------

# 5. Data 层

目录：

```text
src/data/
```

负责：

```text
Supabase client
CRUD
查询
Insert
Update
Delete
Realtime subscription
Storage
数据格式转换
数据库错误处理
```

Data 层必须是：

> **唯一可信的数据访问入口。**

推荐：

```js
export async function updateStock(payload) {
    const supabase = getSupabase()

    const { data, error } = await supabase
        .from('stocks')
        .update(payload)
        .eq('id', payload.id)
        .select()
        .single()

    if (error) throw error

    return data
}
```

禁止业务组件直接：

```js
supabase.from(...)
```

------------------------------------------------------------------------

# 6. 数据唯一来源原则

一个业务数据必须明确：

```text
唯一数据库来源
```

例如：

```text
auction_watchlist
```

就是正式竞价列表数据源。

禁止：

```text
A 看板读新表
B 看板读旧表
C 看 allData
D 看 localStorage
```

然后认为它们“差不多”。

禁止同时存在：

```text
Supabase
allData
Pinia
local cache
组件 ref
```

五套“真相”。

如果 `allData` 只是历史兼容层，必须明确其身份：

```text
cache
view model
store
```

之一，并逐步移除。

------------------------------------------------------------------------

# 7. Pinia 使用规范

Pinia 用于：

```text
跨组件共享状态
当前用户
当前日期
当前选中的股票
全局 UI 状态
Realtime 更新后的共享状态
跨看板需要共享的数据
```

Pinia 不应该成为：

> **第二个数据库。**

禁止把所有 Supabase 数据无条件复制一份长期存入 Store。

核心关系：

```text
数据库 = 持久化真相
Pinia = 当前应用状态
Logic = 业务规则
UI = 展示
```

### Pinia 性能边界

大型数据集合不能因为“方便”全部塞进一个巨大 Store。

必须考虑：

```text
模块化 Store
Map / ID 索引
增量更新
局部更新
分页
缓存
```

避免：

```js
store.items = [...newItems]
```

在每次小变化时整体替换大型数组。

------------------------------------------------------------------------

# 8. localStorage 红线

localStorage 只能用于：

```text
UI 偏好
临时输入缓存
调试标记
其他非业务数据
```

禁止存储：

```text
核心词库
股票数据
竞价数据
题材分组
用户配置
看板业务状态
标签数据
排名数据
记忘数据
任何需要跨设备同步的业务数据
```

所有业务数据必须持久化到 Supabase。

正确方向：

```text
读取：
Supabase → 内存状态 / Store → UI

写入：
UI → Logic → Data → Supabase
                    ↓
                 成功结果
                    ↓
               Store / State
                    ↓
                    UI
```

换设备、清缓存、换浏览器后，业务数据不能丢失。

------------------------------------------------------------------------

# 9. EventBus 规范

EventBus 只能用于：

> **真正的跨模块事件通知。**

例如：

```text
Realtime 更新
 ↓
emit('auction-refresh')
```

禁止用 EventBus 替代正常状态管理。

优先级：

```text
父子组件：
props / emits

跨组件状态：
Pinia

跨业务模块事件：
EventBus

数据库变化：
Realtime
```

职责必须明确。

------------------------------------------------------------------------

# 10. 数据保存与错误处理红线

所有：

```text
save()
update()
insert()
delete()
sync()
```

必须明确返回：

```text
success
或
error
```

禁止静默失败。

正确：

```text
UI 修改
 ↓
Logic
 ↓
Data
 ↓
Supabase 成功
 ↓
返回成功结果
 ↓
Store 更新
 ↓
UI 更新
```

如果保存失败：

```text
UI 必须知道失败
```

至少：

```text
toast
error state
console error
```

不能让用户看到“看起来保存成功”，但实际没有写入数据库。

### 错误处理

禁止：

```js
try {
    await save()
} catch (e) {
}
```

也禁止：

```js
catch {
    return []
}
```

必须区分：

```text
SUCCESS + []
```

和：

```text
ERROR
```

数据库读取失败不能伪装成空数据。

------------------------------------------------------------------------

# 11. 删除安全红线

以下都属于高风险操作：

```text
delete
remove
批量删除
同步删除
清空
覆盖
替换
```

统一原则：

```text
判断 → 保护 → 执行 → 验证
```

绝对禁止：

```text
读取失败
 ↓
[]
 ↓
认为用户没有数据
 ↓
同步 []
 ↓
删除云端全部数据
```

读取失败必须：

```text
ERROR
 ↓
停止同步
```

### 异常快照保护

例如：

```text
正常云端：1000 条
本地异常：10 条
```

不能认为用户删除了 990 条。

必须触发：

```text
_suspiciousWipe
```

保护逻辑。

项目已有的批量删除防护必须长期保留，任何修改同步逻辑的人都不得绕过。

### 删除结果验证

任何批量删除必须：

```text
执行 delete
 ↓
检查 Supabase response
 ↓
确认 affected rows / 结果
 ↓
验证结果符合预期
```

如果预期删除 10，实际异常删除 500：

```text
报警
停止后续操作
```

高价值数据能使用软删除时优先考虑：

```text
deleted_at
```

必须物理删除的表，也要具备备份 / 导出 / 恢复能力。

------------------------------------------------------------------------

# 12. 数据同步规范

任何同步流程：

```text
本地
 ↓
云端
```

不得直接：

```text
比较
 ↓
delete
 ↓
insert
```

推荐：

```text
读取云端
 ↓
读取本地
 ↓
校验
 ↓
计算 diff
 ↓
检查异常比例
 ↓
生成操作计划
 ↓
确认计划安全
 ↓
执行 update / insert / delete
 ↓
重新读取云端
 ↓
验证结果
```

任何同步函数修改前必须回答：

```text
1. 数据从哪里来？
2. 数据写到哪里？
3. 什么情况下认为读取失败？
4. 什么情况下允许删除？
5. 失败后怎么恢复？
```

回答不清楚：

> **禁止直接修改同步代码。**

------------------------------------------------------------------------

# 13. Migration 规范

所有 Migration 必须幂等：

```text
执行一次 = 正常
执行两次 = 结果不变
执行十次 = 结果仍然不变
```

禁止：

```text
每次启动都重复 insert
启动时自动 delete + 重建
```

Migration 不应该由 UI 随意触发。

推荐：

```text
App
 ↓
bootstrap
 ├── session
 ├── migration
 ├── realtime
 └── initial data
```

UI 不应该知道数据库迁移细节。

------------------------------------------------------------------------

# 14. App.vue 规范

App.vue 应尽量轻量：

```vue
<template>
  <AppShell>
    <RouterView />
  </AppShell>
</template>
```

初始化使用：

```js
useAppBootstrap()
```

不要继续把：

```text
登录
Migration
Realtime
股票同步
竞价同步
热点同步
数据迁移
VisibilityChange
```

全部堆进 App.vue。

------------------------------------------------------------------------

# 15. 业务模块标准结构

推荐：

```text
src/
├── views/
│   └── AuctionBoard.vue
│
├── components/
│   └── auction/
│
├── logic/
│   └── auction/
│       ├── auction-workflow.js
│       ├── auction-validation.js
│       └── auction-sync.js
│
├── data/
│   └── auction/
│       ├── auction-data.js
│       └── auction-realtime.js
│
└── stores/
    └── auction-store.js
```

核心思想：

```text
一个业务模块
=
View
+
Logic
+
Data
+
必要的 Store
```

不要求每个模块机械创建所有文件，但职责必须保持清晰。

------------------------------------------------------------------------

# 16. 纯 Vue3 架构红线

禁止：

```js
window.xxx = ...
window.app.xxx()
window.remainingBoards
globalThis.xxx
```

禁止：

```js
(function () {
   ...
})()
```

作为业务模块。

禁止：

```html
<script src="xxx.js"></script>
```

绕过 ES Module。

禁止：

```text
CDN Vue
CDN Supabase
旧 IIFE 业务模块
globalProperties 业务注入
```

### remaining-boards.js

如果仍存在：

```text
必须迁移为标准 ES Module + Data / Logic
```

如果功能已被新模块替代：

```text
直接删除
```

不能保持“半旧半新”的隐藏状态。

### app-core.js

不得继续无限增长。

应按业务拆分：

```text
logic/
├── app/
├── stocks/
├── auction/
├── bidding/
├── jiwang/
├── hotspot/
├── pattern/
├── rank/
├── emotion/
├── etf/
└── stats/
```

------------------------------------------------------------------------

# 17. UI 响应式原则

Vue3 的响应式原则必须明确：

> **响应式保证状态变化能够驱动 UI 更新，但不代表任何状态变化都天然高性能。**

正确：

```text
Data 成功
 ↓
Store / reactive state 更新
 ↓
Vue 自动渲染
```

不要依赖：

```text
setInterval
setTimeout
重新进入页面
切换日期
```

来“碰巧刷新”。

同时必须控制：

```text
状态影响范围
计算影响范围
组件更新范围
DOM 更新范围
```

核心原则：

```text
一个 Cell 变化
→ 不应该导致整个 Row 重算

一个 Row 变化
→ 不应该导致整个列表重算

一个列表变化
→ 不应该导致整个看板重算

一个看板变化
→ 不应该导致其他看板重算

一个页面变化
→ 不应该导致整个 App 重算
```

------------------------------------------------------------------------

# 18. Vue3 响应式边界

响应式状态优先按照：

```text
全局状态
 ↓
业务模块状态
 ↓
页面状态
 ↓
组件状态
 ↓
列表项状态
 ↓
单元格状态
```

进行隔离。

禁止为了方便，把：

```text
stocks
auction
bidding
rank
topics
history
charts
statistics
settings
```

全部塞进一个巨大响应式对象。

原则：

> **状态应该靠近真正使用它的模块。**

------------------------------------------------------------------------

# 19. Computed 规范

`computed` 用于：

```text
派生状态
轻量计算
稳定依赖
```

禁止在高频 computed 中反复执行大型：

```text
filter
sort
map
reduce
find
复杂对象创建
重复图表数据生成
```

如果数据量较大或更新频繁，必须评估：

```text
缓存
索引
预计算
增量更新
分页
虚拟列表
```

原则：

> **不要让一个高频响应式状态成为大型 computed 链的根节点。**

------------------------------------------------------------------------

# 20. Watch 规范

`watch` 不是万能同步工具。

禁止：

```text
一个状态
 ↓
多个 watch
 ↓
不同 watch 分别请求数据
 ↓
互相触发
```

大型对象禁止无目的：

```js
{ deep: true }
```

使用 watch 前必须回答：

```text
监听谁？
为什么监听？
谁会触发？
触发后做什么？
是否可能重复触发？
是否可能循环？
是否会触发网络请求？
是否会触发大型计算？
```

------------------------------------------------------------------------

# 21. Template 性能规范

Template 只负责展示。

禁止在模板中执行重型业务计算：

```vue
{{ calculateRank(stock) }}
{{ calculateTopics(stock) }}
{{ calculateStatistics(stock) }}
{{ getComplexData(stock) }}
```

尤其不能在这些函数中执行：

```text
filter
find
sort
map
reduce
数据库查询
复杂业务计算
```

推荐：

```text
Data
 ↓
Logic
 ↓
ViewModel / computed
 ↓
Template
```

------------------------------------------------------------------------

# 22. 高频数据与增量更新

以下数据属于高频数据：

```text
Realtime
竞价变化
行情变化
动态排名
倒计时
实时统计
动态状态
```

禁止默认：

```text
收到一条变化
 ↓
整个 Store 更新
 ↓
整个页面重算
```

推荐：

```text
Realtime
 ↓
识别变化记录
 ↓
计算 diff
 ↓
更新对应数据
 ↓
最小范围响应式更新
```

如果短时间内变化很多：

```text
变化
变化
变化
变化
 ↓
批量合并
 ↓
一次 Store 更新
 ↓
一次 UI 更新
```

可根据实际业务使用：

```text
批处理
requestAnimationFrame
节流
合并更新
```

禁止为了“不卡”而随意堆 `setTimeout()`。

------------------------------------------------------------------------

# 23. 大数组更新规范

对于：

```text
股票
竞价
排名
历史记录
题材
行情
```

如果只变化一条：

```text
stock A changed
```

优先：

```text
定位 stock A
 ↓
只更新 stock A
 ↓
依赖 stock A 的 UI 更新
```

而不是：

```text
重新生成全部数据
 ↓
整体替换大型数组
 ↓
整个列表重新参与响应式更新
```

允许整体替换：

```text
首次加载
明确的数据集切换
用户主动完整刷新
日期切换后的完整数据源切换
```

但仍必须检查计算和 DOM 重建成本。

------------------------------------------------------------------------

# 24. 页面生命周期与导航

底部导航、Router、Tab 切换必须考虑生命周期。

禁止默认：

```text
切换页面
 ↓
销毁
 ↓
重新创建
 ↓
重新查询
 ↓
重新计算
 ↓
重新初始化图表
 ↓
重新订阅 Realtime
```

页面再次进入时，应优先：

```text
复用已有状态
```

需要刷新时：

```text
明确调用 refresh()
```

而不是：

```text
重新进入页面 = 刷新一切
```

页面是否使用 KeepAlive 必须根据：

```text
内存占用
组件数量
图表数量
Realtime subscription
事件监听
```

决定。

------------------------------------------------------------------------

# 25. 底部导航性能红线

底部导航是全局高频入口。

点击不同看板时，不允许产生无意义的：

```text
全局数据重新加载
全部 Store 重建
全部 computed 重算
全部图表重新初始化
重复 Realtime subscription
重复 API 请求
```

首次进入：

```text
初始化
```

再次进入：

```text
优先复用
```

真正刷新：

```text
明确 refresh()
```

------------------------------------------------------------------------

# 26. 日期切换性能规范

日期属于高影响状态。

例如：

```text
2026-08-11
 ↓
2026-08-12
```

不能默认造成：

```text
所有 Store 更新
 ↓
所有看板重新查询
 ↓
所有 computed 失效
 ↓
所有图表重建
```

必须先判断：

```text
哪些模块真正依赖日期？
```

例如：

```text
Auction
 ↓
刷新

History
 ↓
刷新

User Settings
 ↓
不刷新

静态配置
 ↓
不刷新
```

因此：

> **全局日期变化不能成为整个 App 的“重新计算按钮”。**

------------------------------------------------------------------------

# 27. 分页性能规范

分页必须尽可能是：

> **显示窗口切换，而不是整个数据集重新计算。**

例如：

```text
总数据：2000
每页：20
```

目标：

```text
数据：2000
 ↓
当前页：20
 ↓
实际渲染：约 20
```

禁止因为翻页：

```text
page = 2
 ↓
重新处理 2000 条
 ↓
filter
 ↓
sort
 ↓
map
 ↓
复杂计算
 ↓
最后 slice 20 条
```

如果排序和筛选属于数据业务逻辑，应考虑：

```text
提前计算
缓存
索引
服务器端查询
```

------------------------------------------------------------------------

# 28. 大列表与虚拟化

以下模块必须评估虚拟列表：

```text
股票列表
竞价列表
历史记录
排名列表
大型表格
日志
数据明细
```

原则：

```text
数据数量 ≠ DOM 数量
```

例如：

```text
数据：2000
实际 DOM：约 30～60
```

具体数量根据 UI 和滚动方式决定。

如果数据量较小，不必为了“虚拟化”而增加不必要复杂度。

------------------------------------------------------------------------

# 29. Row / Cell 组件边界

大型列表优先：

```text
List
 ↓
Row
 ↓
Cell
```

一个 Row 应尽量只依赖自己的数据。

例如：

```text
Stock A 更新
```

不应该导致：

```text
Stock B
Stock C
Stock D
...
```

全部执行复杂计算。

复杂 Row 中的：

```text
图表
标签
历史走势
弹窗
Tooltip
```

应进一步拆分并按需加载。

------------------------------------------------------------------------

# 30. 图表性能规范

图表必须区分：

```text
创建
初始化
更新
销毁
```

正常：

```text
组件 mount
 ↓
创建 chart
```

数据变化：

```text
更新 chart 数据
```

真正销毁：

```text
dispose
```

禁止：

```text
每次数据变化
 ↓
destroy
 ↓
new chart
```

多个图表必须评估：

```text
初始化数量
数据点数量
刷新频率
页面切换
KeepAlive
内存占用
```

------------------------------------------------------------------------

# 31. Realtime 生命周期

Realtime 负责：

> **跨页面 / 跨设备 / 跨会话的数据变化通知。**

本页面自己保存：

```text
保存成功
 ↓
立即更新自己的 Store
```

不应该等待 Realtime 才显示。

跨设备：

```text
A 保存
 ↓
Supabase
 ↓
Realtime
 ↓
B 收到变化
 ↓
B Store
 ↓
B UI
```

### 防重复更新

必须防止：

```text
用户自己保存
 ↓
Store 更新一次
 ↓
Realtime 又更新一次
 ↓
重复处理
```

根据业务选择：

```text
乐观更新
```

或：

```text
服务器结果更新
```

必须有明确策略，不能两个体系混用到无法判断。

### Subscription 生命周期

禁止：

```text
进入页面 → subscribe
再次进入 → 再 subscribe
第三次进入 → 再 subscribe
```

必须保证：

```text
subscribe
 ↓
明确持有 channel
 ↓
模块统一管理
 ↓
离开时 unsubscribe
```

如果页面使用 KeepAlive，则由模块统一管理 subscription 生命周期。

不能由多个组件重复订阅同一业务数据。

------------------------------------------------------------------------

# 32. 网络请求性能

禁止同一页面的多个组件分别请求同一业务数据：

```text
StockRow A → 请求股票
StockRow B → 请求股票
StockRow C → 请求股票
```

优先：

```text
父级 / 模块统一加载
Store
Data 层复用
请求去重
缓存
```

同一业务数据短时间重复请求时，必须明确：

```text
是否复用已有请求
是否复用缓存
是否需要重新请求
```

------------------------------------------------------------------------

# 33. 首次加载、刷新与 Loading

必须区分：

```text
首次加载
```

和：

```text
后续刷新
```

首次加载：

```text
初始化
获取必要数据
建立必要状态
```

后续刷新：

```text
只更新需要更新的数据
```

禁止：

```text
每次刷新 = 重新初始化整个页面
```

### Loading

局部数据刷新时：

```text
局部 loading
```

而不是：

```text
整个 App loading
```

原则：

> **Loading 范围必须与数据影响范围一致。**

------------------------------------------------------------------------

# 34. UI 状态与业务数据分离

以下属于 UI State：

```text
当前页
展开行
选中股票
Modal
Tooltip
Hover
长按
Tab
Loading
```

以下属于业务数据：

```text
股票数据
竞价数据
历史数据
排名数据
```

不要混成一个巨大对象。

例如：

```text
uiStore.currentPage
```

与：

```text
auctionStore.stocks
```

职责分离。

这样：

```text
翻页
```

不会导致：

```text
股票业务数据重新加载
```

------------------------------------------------------------------------

# 35. 性能问题定位流程

以后出现：

```text
卡顿
掉帧
点击延迟
页面切换慢
日期切换慢
分页慢
Realtime 卡顿
图表卡顿
```

禁止直接打补丁。

必须先判断属于：

```text
P1 数据请求问题
P2 响应式更新问题
P3 Computed / Watch 计算问题
P4 DOM 渲染问题
P5 Layout / Paint 问题
P6 图表问题
P7 重复请求
P8 重复 Realtime
P9 生命周期 / 内存问题
```

然后再修改。

禁止用：

```text
setTimeout
debounce
throttle
nextTick
```

作为第一反应来掩盖架构问题。

这些工具可以使用，但必须建立在明确原因之上。

------------------------------------------------------------------------

# 36. 性能红线

以下行为属于架构级性能问题：

```text
❌ 一个小状态变化导致整个 App 重算

❌ 一个股票变化导致整个列表重算

❌ 一个日期变化导致所有看板重新加载

❌ 一个 Tab 切换导致所有页面重新初始化

❌ 翻页导致整个数据集重复计算

❌ Realtime 单条变化导致全量数据替换

❌ 大型对象 deep watch

❌ 模板中执行重型计算

❌ 大量 DOM 无评估虚拟化

❌ 每次数据变化重新创建图表

❌ 页面重复建立 Realtime subscription

❌ 相同数据重复请求

❌ 用延迟执行掩盖真实性能问题

❌ 为解决局部卡顿而扩大全局 Store

❌ 为了“简单”把所有数据塞进 Pinia
```

------------------------------------------------------------------------

# 37. 新功能影响范围审查

以后新增任何功能，在写代码前必须明确：

```text
【新增状态】
新增哪些 ref / reactive / Pinia？

【数据来源】
来自哪里？

【影响组件】
哪些组件依赖？

【计算范围】
哪些 computed / watch 会重新计算？

【DOM 范围】
哪些 DOM 会变化？

【网络】
会增加哪些请求？

【Realtime】
会增加哪些订阅？

【生命周期】
什么时候创建？
什么时候销毁？

【数据量】
最大可能多少条？

【性能风险】
低 / 中 / 高
```

如果无法回答：

> **不得直接开始写代码。**

------------------------------------------------------------------------

# 38. Bug 修复流程

以后修 Bug 必须：

```text
① 复现
 ↓
② 定位 View / Logic / Data
 ↓
③ 找到真实数据源
 ↓
④ 确认调用链
 ↓
⑤ 判断响应式 / 生命周期 / 性能影响
 ↓
⑥ 修改最小范围
 ↓
⑦ npm run build
 ↓
⑧ 功能回归
 ↓
⑨ 刷新页面验证
 ↓
⑩ 多设备 / Realtime 验证
 ↓
⑪ 性能回归
```

禁止：

```text
看到报错
 ↓
直接加一个 if
```

------------------------------------------------------------------------

# 39. AI 修改规范

以后让 AI 修改项目时，必须把本文件一起提供给 AI。

AI 必须遵守：

```text
1. 不恢复旧 Vue/JS 架构
2. 不增加 window 全局 API
3. 不直接在 UI 写 Supabase
4. 不跳过 Logic 层
5. 不把业务逻辑塞进 App.vue
6. 不删除数据保护机制
7. 不把读取失败转换为空数组
8. 不让保存失败静默
9. 不绕过 Data Module
10. 不为了修一个 Bug 破坏三层架构
11. 不扩大无关响应式影响范围
12. 不制造重复请求或重复 Realtime subscription
13. 不以全量重算解决局部状态变化
14. 不以 setTimeout 等方式掩盖根因
```

### AI 修改前必须输出

```text
【问题定位】
发生在哪一层？

【当前调用链】
UI → Logic → Data → Backend

【真实数据源】
是哪张表 / 哪个 API？

【影响范围】
哪些看板 / 组件？

【响应式影响】
哪些状态会变化？

【重新计算范围】
哪些 computed / watch 会触发？

【渲染影响】
哪些组件 / DOM 会更新？

【网络影响】
是否增加请求？

【Realtime 影响】
是否增加订阅？

【生命周期】
是否存在重复 mount / subscribe / chart 初始化？

【是否涉及写入】
是 / 否

【是否涉及删除】
是 / 否

【是否涉及迁移】
是 / 否

【风险等级】
P0 / P1 / P2 / P3

【性能风险】
低 / 中 / 高
```

然后再修改。

------------------------------------------------------------------------

# 40. AI 不得凭猜测修改数据库逻辑

如果 AI 不确定：

```text
表名
字段名
数据来源
同步关系
删除规则
```

必须先搜索项目代码。

禁止：

```text
猜一个字段
猜一个表
猜一个 API
```

修改 Data 层后至少验证：

```text
成功
失败
空数据
重复数据
异常数据
刷新
Realtime
```

修改删除逻辑后额外验证：

```text
正常删除
批量删除
空数据删除
读取失败
网络失败
云端为空
本地为空
本地异常少
本地异常多
```

永久红线：

```text
读取失败 ≠ 空数据
```

------------------------------------------------------------------------

# 41. Git 提交规范

推荐：

```text
fix:
feat:
refactor:
data:
migration:
security:
performance:
```

例如：

```text
fix: 修复 HomeStocksView 编辑保存链路
fix: 阻止异常同步触发批量删除
refactor: 拆分 app-core stocks workflow
data: 迁移 remaining-boards 为 ES module
security: 加强批量删除保护
performance: 限制竞价列表响应式更新范围
```

------------------------------------------------------------------------

# 42. 上线与功能完成验收

## 架构

- [ ] 全部 UI 使用 `.vue`
- [ ] 没有业务 `window.*`
- [ ] 没有 `innerHTML`
- [ ] 没有 CDN Vue
- [ ] 没有 CDN Supabase
- [ ] 没有旧 IIFE 业务模块
- [ ] 没有 globalProperties 业务注入
- [ ] 没有绕过 UI / Logic / Data 的调用链

## 数据

- [ ] 所有核心看板能读取
- [ ] 所有核心看板能新增
- [ ] 所有核心看板能编辑
- [ ] 所有核心看板能删除
- [ ] 保存失败可见
- [ ] 刷新后数据仍存在
- [ ] 换设备数据仍存在

## 安全

- [ ] 批量删除有保护
- [ ] 异常数据不会触发清空
- [ ] 读取失败不会变成空数据
- [ ] 空数据不会自动删除云端
- [ ] Migration 幂等
- [ ] Delete 有结果确认

## 同步

- [ ] Realtime 正常
- [ ] 新增同步
- [ ] 修改同步
- [ ] 删除同步
- [ ] 多设备同步
- [ ] 不重复更新
- [ ] Subscription 不重复

## 性能

- [ ] 底部导航切换无明显卡顿
- [ ] 顶部日期切换无明显卡顿
- [ ] 大列表分页无明显卡顿
- [ ] 筛选 / 排序无明显卡顿
- [ ] Realtime 单条更新不会造成无意义全量刷新
- [ ] 没有重复 Realtime subscription
- [ ] 没有重复请求
- [ ] 大列表已评估虚拟化
- [ ] 图表不会因普通数据变化重复初始化
- [ ] 没有无目的大型 deep watch
- [ ] Template 没有重型业务计算

## 构建

- [ ] `npm run build`
- [ ] 无新增 warning
- [ ] 无循环依赖
- [ ] 无未使用关键模块
- [ ] 无明显 dead code

------------------------------------------------------------------------

# 43. 性能回归测试

任何修改以下内容：

```text
Pinia
computed
watch
Realtime
列表
分页
日期
导航
图表
全局状态
```

都必须进行性能回归。

至少测试：

```text
① 底部导航切换
② 顶部日期切换
③ 大列表翻页
④ 筛选
⑤ 排序
⑥ 展开 / 关闭 Row
⑦ 图表打开
⑧ 图表关闭
⑨ Realtime 更新
⑩ 页面刷新
```

不能因为：

```text
功能正常
```

就认为：

```text
修改完成
```

------------------------------------------------------------------------

# 44. 当前项目风险与历史规则

本节保留原 V2 中与当前项目状态直接相关的历史风险判断，但它们属于**当前审查结论**，不是永久架构规则。

例如：

```text
remaining-boards.js
HomeStocksView
重点看板持久化
删除安全
Realtime
```

如果这些问题已经修复：

> 必须更新本节，不得继续把旧问题当成当前问题。

因此：

> **架构规范描述长期规则；项目风险记录当前状态。两者必须分开。**

------------------------------------------------------------------------

# 45. 长期目标架构

最终项目保持：

```text
                         Vue3
                           │
                 ┌─────────┴─────────┐
                 │                   │
               Router              Pinia
                 │                   │
                 └─────────┬─────────┘
                           │
                          UI
                           │
                        Logic
                           │
                    ViewModel / VM
                           │
                         Data
                           │
                      Supabase/API
                           │
                 ┌─────────┴─────────┐
                 │                   │
              Database            Realtime
```

并且：

```text
没有 window 业务全局变量
没有旧 IIFE
没有 CDN 依赖
没有 UI 直接操作数据库
没有静默保存失败
没有读取失败伪装为空数据
没有异常同步直接清空云端
没有未经保护的批量删除
没有无限增长的 app-core
没有无边界响应式更新
没有无意义全量渲染
没有重复请求
没有重复 Realtime subscription
```

------------------------------------------------------------------------

# 46. V3 最终原则

以后这个项目无论：

```text
修 Bug
加功能
重构
增加看板
增加字段
增加筛选
增加分页
增加图表
修改日期
修改导航
修改同步
修改 Realtime
修改 Supabase
```

都必须遵守以下原则：

### ① Vue3 是唯一前端架构

不恢复旧架构。

### ② UI 只负责展示和交互

不直接操作数据库。

### ③ Logic 负责业务

业务规则不能散落在 UI。

### ④ Data 是数据库唯一入口

所有 Supabase CRUD 从 Data 层进入。

### ⑤ 数据库是持久化真相

Pinia 不是数据库，localStorage 不是数据库。

### ⑥ 业务数据必须云端持久化

换设备、清缓存、换浏览器不能丢失业务数据。

### ⑦ 保存必须有结果

不能静默失败。

### ⑧ 读取失败绝不能当空数据

这是数据安全红线。

### ⑨ 空数据绝不能自动触发批量删除

这是数据安全红线。

### ⑩ 所有危险删除必须有护栏

尤其是批量同步和异常快照。

### ⑪ 响应式必须有边界

局部状态变化不能无意义影响整个 App。

### ⑫ 更新优先增量化

能更新一条，就不要无意义替换一千条。

### ⑬ 数据量与 DOM 数量必须分离

大型列表必须评估虚拟化、分页和按需渲染。

### ⑭ 页面生命周期必须明确

不能反复 mount、请求、订阅、初始化图表。

### ⑮ Realtime 必须可控

不能重复订阅、重复处理、重复请求。

### ⑯ 图表优先复用

数据变化优先更新实例，而不是重复创建。

### ⑰ 性能问题必须先定位

不能靠延迟执行掩盖架构问题。

### ⑱ Bug 修复不能破坏架构

宁可补齐 Vue3 模块，也不要把旧代码重新挂回来。

------------------------------------------------------------------------

# 47. 本文件使用方式

建议项目根目录保存：

```text
ARCHITECTURE.md
```

以后每次修改项目：

```text
开发者 / AI
      ↓
先读 ARCHITECTURE.md
      ↓
定位问题
      ↓
判断 UI / Logic / Data
      ↓
判断数据安全影响
      ↓
判断响应式 / 生命周期 / 性能影响
      ↓
修改
      ↓
执行验收
```

如果未来项目进入新的架构阶段，可以继续升级：

```text
ARCHITECTURE.md
```

但不得降低本文件中的：

```text
数据安全红线
纯 Vue3 红线
三层依赖方向
唯一数据来源
禁止旧全局架构
响应式边界
性能红线
```

------------------------------------------------------------------------

# 48. 当前阶段的一句话定义

> **vue3biga 的 Vue3 迁移已经结束，现在是纯 Vue3 长期维护与持续新增功能阶段；以后所有代码修改都必须同时保证功能正确、数据安全、架构不走偏、响应式可控和性能可持续。**
