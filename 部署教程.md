# 部署到 GitHub Pages 小白教程

> 本教程教你把一个 Vue3 项目推送到 GitHub，并自动部署成网页。
> 按顺序一步步做，**每一步都不要跳过**，尤其是「代理」那一步，跳了会卡很久。

---

## 0. 你需要先准备好的东西

1. **GitHub 账号**（去 https://github.com 注册）
2. **Git**（去 https://git-scm.com 下载安装，一路下一步就行）
3. **Node.js**（去 https://nodejs.org 下载 LTS 版本，一路下一步）
4. **VPN / 代理软件**（比如 Clash、v2rayN 等，能翻墙访问 GitHub）

装完 Git 后，打开 PowerShell（Win 键 + R，输入 `powershell` 回车），输入：
```powershell
git --version
```
能看到版本号说明装好了。

---

## 1. 在 GitHub 上新建仓库

1. 登录 https://github.com ，点右上角 **`+`** → **New repository**
2. Repository name 填一个名字，比如 `my-app`
3. 选 **Public**（公开，否则 Pages 免费版用不了）
4. **不要**勾选 "Add a README file"（保持仓库为空，方便后面直接推送）
5. 点 **Create repository**

记住你的仓库名，后面要用。你的仓库地址是：
```
https://github.com/你的用户名/仓库名
```
比如 `https://github.com/jialeok/my-app`

---

## 2. 生成 GitHub Token（钥匙）

推送代码到 GitHub 需要一把"钥匙"，叫 Personal Access Token（PAT）。

1. 点右上角头像 → **Settings** → 左边最底部 **Developer settings**
2. 点 **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
3. 填写：
   - **Token name**：随便起个名，比如 `deploy`
   - **Expiration**：选 90 天或更长
   - **Repository access**：选 **Only select repositories** → 选你刚建的仓库
   - **Permissions** → **Repository permissions**：
     - **Contents**：选 **Read and write**
     - **Workflows**：选 **Read and write**  ← **这个很重要！不选会推不上去**
   - **Permissions** → **Account permissions**：
     - **Pages**：选 **Read and write**
4. 点 **Generate token**
5. **立刻复制 token**（只显示一次！），形如：
   ```
   github_pat_11ASFOV3A0xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   存到记事本里备用。

> ⚠️ **Token 一定要勾选 Workflows 权限！** 否则推送 `.github/workflows/` 下的部署脚本会被拒绝。

---

## 3. 配置代理（最容易卡的一步！）

### 为什么需要代理？

GitHub 在国外，国内直连经常超时、连不上。你的 VPN 软件开了代理，但 **Git 默认不走系统代理**，所以要手动告诉 Git 走代理。

### 怎么知道代理端口？

打开你的代理软件（比如 Clash），找到它的本地监听端口。常见端口：
- **Clash for Windows**：一般是 `7897` 或 `7890`
- **v2rayN**：一般是 `10809` 或 `10808`

在 PowerShell 里输入这行命令测试能不能连上 GitHub：
```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7897"; Invoke-WebRequest "https://api.github.com" -UseBasicParsing -TimeoutSec 10
```

- 如果返回一堆 JSON 内容 → **代理通了**，记住这个端口号
- 如果报错超时 → 换个端口再试（`7890`、`10809`、`10808`），或者检查你的 VPN 是否正常运行

### 设置代理（每次开 PowerShell 都要设一次）

找到能用的端口后，**每次打开新的 PowerShell 窗口**，都要先输入这两行：
```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$env:HTTP_PROXY = "http://127.0.0.1:7897"
```
> 把 `7897` 换成你实际的端口。

设完代理后，测试 Git 能不能连：
```powershell
git ls-remote https://github.com/你的用户名/你的仓库名.git
```
不报错、能返回（哪怕是空的）就说明 Git 走代理成功了。

---

## 4. 配置项目文件

### 4.1 修改 `vite.config.js` 的 base 路径

打开项目根目录的 `vite.config.js`，找到 `base` 这一行：
```javascript
export default defineConfig({
  base: '/你的仓库名/',   // ← 改成你的仓库名，前后都要有斜杠！
  // ...
});
```
比如你的仓库叫 `my-app`，就写 `base: '/my-app/'`。

> ⚠️ **这一步不能漏！** 不改 base，部署后网页会白屏（资源 404）。

### 4.2 确认路由用 Hash 模式

打开 `src/router/index.js`，确认是 `createWebHashHistory`：
```javascript
import { createRouter, createWebHashHistory } from 'vue-router'

const router = createRouter({
  history: createWebHashHistory(),  // ← 用 Hash 模式，GitHub Pages 必须用这个
  routes: [...]
})
```

> ⚠️ 不要用 `createWebHistory()`，GitHub Pages 不支持，会 404。

### 4.3 确认有部署脚本

在项目里创建文件 `.github/workflows/deploy-pages.yml`（一层层建文件夹），内容：
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

这个文件的作用：每次你推送代码到 main 分支，GitHub 会自动帮你 `npm install` + `npm run build`，然后把 `dist` 文件夹部署成网页。

### 4.4 确认 `.gitignore` 没有 ignore 掉 `package-lock.json`

打开 `.gitignore`，**确保里面没有这一行**：
```
package-lock.json   ← 如果有，删掉这行！
```

> ⚠️ `package-lock.json` 必须提交！否则 GitHub Actions 的 `cache: npm` 会报错：
> `Dependencies lock file is not found`

### 4.5 在 GitHub 仓库设置 Pages

1. 打开你的仓库页面 → **Settings** → 左边 **Pages**
2. **Build and deployment** → **Source** 选 **GitHub Actions**
3. 保存

---

## 5. 提交代码并推送

### 5.1 初始化 Git（如果项目还没有 Git）

在项目根目录打开 PowerShell：
```powershell
git init
git branch -M main
```

### 5.2 连接远程仓库

```powershell
git remote add origin https://github.com/你的用户名/你的仓库名.git
```

### 5.3 添加并提交所有文件

```powershell
git add .
git commit -m "初始化项目"
```

### 5.4 推送到 GitHub

**先设代理**（如果换了新窗口）：
```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$env:HTTP_PROXY = "http://127.0.0.1:7897"
```

**用 token 推送**（把 `你的TOKEN` 和 `你的用户名/你的仓库名` 换成实际值）：
```powershell
git push --force "https://你的TOKEN@github.com/你的用户名/你的仓库名.git" main
```

> - `--force` 是强制覆盖远程。如果远程仓库是空的（没勾 README），可以不加 `--force`。
> - 如果远程有内容（比如自动建了 README），**加 `--force` 覆盖它**。
> - 推送成功会显示类似：`main -> main` 或 `main -> main (forced update)`

### 5.5 推送失败怎么办？

| 报错信息 | 原因 | 解决 |
|---------|------|------|
| `Failed to connect to github.com port 443` | 没设代理或代理端口错 | 回到第 3 步重新设代理 |
| `Empty reply from server` | 网络抖动 | 重试一次推送命令 |
| `refusing to allow a PAT to create or update workflow` | Token 没有 workflow 权限 | 回到第 2 步重新生成 token，勾选 Workflows |
| `Dependencies lock file is not found` | `package-lock.json` 被 ignore 了 | 回到第 4.4 步，从 `.gitignore` 删掉 `package-lock.json`，然后 `git add package-lock.json` 重新提交推送 |
| `fetch first` / `non-fast-forward` | 远程有本地没有的提交 | 推送命令加 `--force` |

---

## 6. 查看部署状态

### 6.1 看 Actions 有没有跑成功

1. 打开仓库页面 → 顶部 **Actions** 标签
2. 会看到一个叫 "Deploy to GitHub Pages" 的工作流
3. 等它跑完（1~2 分钟），显示 **绿色对勾 ✅** = 成功
4. 如果是 **红色叉叉 ❌** = 失败，点进去看哪一步红了，对照第 5.5 节排错

### 6.2 访问你的网页

部署成功后，访问：
```
https://你的用户名.github.io/你的仓库名/
```
比如 `https://jialeok.github.io/my-app/`

> 注意 URL 末尾有 `/`，而且 `/#/` 后面是路由（Hash 模式）。

---

## 7. 以后改了代码怎么更新？

每次改完代码，三步走：

```powershell
# 1. 设代理（新窗口才需要）
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$env:HTTP_PROXY = "http://127.0.0.1:7897"

# 2. 提交
git add .
git commit -m "说明你改了什么"

# 3. 推送（不用 --force 了，因为远程已经有代码了）
git push "https://你的TOKEN@github.com/你的用户名/你的仓库名.git" main
```

推送后 GitHub 会自动重新构建部署，等 1~2 分钟刷新网页就能看到更新。

---

## 常见问题

**Q: 网页打开是白屏？**
A: 检查 `vite.config.js` 的 `base` 是否 = `/仓库名/`，路由是否用 `createWebHashHistory()`。

**Q: Actions 报错 "Setup Node" 失败？**
A: `package-lock.json` 没提交。从 `.gitignore` 删掉它，`git add package-lock.json` 重新提交推送。

**Q: 推送报 "workflow scope" 错误？**
A: Token 没勾 Workflows 权限。重新生成 token，勾上 Workflows: Read and write。

**Q: 推送一直超时连不上？**
A: 代理没设或端口不对。在 PowerShell 里先设 `$env:HTTPS_PROXY`，用 `Invoke-WebRequest` 测试端口能通再用 git。

**Q: 每次开 PowerShell 都要重新设代理？**
A: 是的，环境变量只在当前窗口有效。嫌麻烦可以设永久环境变量（系统设置里），但不推荐（关了 VPN 会影响其他程序）。

---

## 一句话总结

```
建仓库 → 生成 token(勾 workflows) → 设代理 → 改 base 路径 → git push → 等 Actions 跑完 → 访问网页
```